# API Base URL
# In production (docker-compose), this should be the Caddy reverse proxy URL
# In development, this should be the API server URL
NEXT_PUBLIC_API_URL=http://localhost:80/api

# Socket.io URL
# In production (docker-compose), this should be the Caddy reverse proxy URL
# In development, this should be the API server URL (Socket.io runs on same server as API)
NEXT_PUBLIC_SOCKET_URL=http://localhost:80

# LiveKit Server URL
# In production (docker-compose), this should be the LiveKit server URL
# In development, this should be the LiveKit server URL
NEXT_PUBLIC_LIVEKIT_URL=ws://localhost:7880

